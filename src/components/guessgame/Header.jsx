
const Header = ({guessLeft}) => {
  return (
    <header>
      <h1>Guess Fun Game</h1>
      <p>
        Guess the word with {guessLeft} attempts to keep the programming world safe from Assembly.
      </p>
    </header>
  )
}

export default Header
